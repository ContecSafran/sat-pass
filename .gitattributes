# 줄바꿈은 저장소 안에서 LF 로 통일한다.
# (특히 gradlew 가 CRLF 로 커밋되면 리눅스 CI 에서 실행되지 않는다.)
* text=auto eol=lf

# 윈도우 전용 스크립트는 CRLF 유지
*.bat text eol=crlf
*.cmd text eol=crlf

# 바이너리
*.jar binary
*.apk binary
*.aab binary
*.png binary
*.jpg binary
*.webp binary
*.keystore binary
*.jks binary
